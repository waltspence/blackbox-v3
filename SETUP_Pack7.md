# Setup — Pack #7 (CrowdLens v1)

```bash
python3 scripts/crowd_blend.py \
  --bestlines outputs/bestlines.json \
  --crowd_csv templates/crowd_sources_sample.csv \
  --config configs/crowd.yaml
```